{
  "name": "official-checklist",
  "version": "0.2.0",
  "private": true,
  "description": "Machine-verifiable OKF checklists for official government and consular processes.",
  "scripts": {
    "validate:okf": "tsx scripts/validate-okf.ts",
    "validate:data": "tsx scripts/validate-questionnaires.ts",
    "validate": "npm run validate:okf && npm run validate:data",
    "test": "npm run validate"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "yaml": "^2.6.1",
    "zod": "^3.24.1"
  }
}
